package com.cleancodeschool.before;

/**
 * BEFORE APPLYING EXTRACT INTERFACE REFACTOR
 */
public class ImageController {
    public String findById(final Long id) { return "Image with id: " + id.toString();};
}