const SPEED_OF_LIGHT = 3 * Math.pow(10, 8);
const PROTON_WEIGHT_UMA = 1.0076;
const NEUTRON_WEIGHT_UMA = 1.0089;
const UMA_TO_KG = 1.66 * Math.pow(10, -27);
const JULE_TO_MEV = 6.242 * Math.pow(10, 12);

module.exports = {
  SPEED_OF_LIGHT,
  PROTON_WEIGHT_UMA,
  NEUTRON_WEIGHT_UMA,
  UMA_TO_KG,
  JULE_TO_MEV
};
